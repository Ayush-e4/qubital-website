import ContactContent from './ContactContent';

export const metadata = {
  title: 'Contact | Qubital',
  description: 'Start a conversation with Qubital for your digital transformation needs. Reach out to our principal engineering team to discuss your architectural requirements.',
};

export default function ContactPage() {
  return <ContactContent />;
}
